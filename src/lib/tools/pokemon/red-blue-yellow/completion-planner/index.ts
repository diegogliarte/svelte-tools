import type { ToolDefinition } from '$lib/tools/types';

export const tool: ToolDefinition = {
	title: 'Completion Planner',
	description: 'Track all 151 Pokémon with a clean, persistent Pokédex grid.',
	fullscreen: true,
	order: 2
};
